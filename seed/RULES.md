# Handoff Rules v3.0

- PowerShell-first. Provide numbered steps. Include 'What to expect' after each step.
- Full files only for code changes. No diff patches.
- Keep Brand Template to 11 headers listed in AI_README.
- Admin-only endpoints for import/commit.
- Use Cloudflare Pages project **bc** for deploys. Verify via public URLs.
- Prefer simple, readable JS and clear reports.
- Never rename or move files unless asked. Keep paths stable.
- Keep logs minimal and data-safe. No secrets in repo.
- This file is the current source of truth for handoffs.